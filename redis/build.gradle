plugins {
    id 'java'
}

group 'net.ibxnjadev.vmessenger.redis'
version '0.1.5'

dependencies {
    implementation 'redis.clients:jedis:3.7.0'
    compileOnly 'com.fasterxml.jackson.core:jackson-databind:2.12.5'
    compileOnly project(":universal")
}

test {
    useJUnitPlatform()
}